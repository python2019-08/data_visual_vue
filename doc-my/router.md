为了实现这个目标，我们需要进行一次小规模的重构。这主要涉及到引入 Vue 的官方路由库 vue-router。下面是我的计划，我会引导您一步步完成：

> 1. 安装 vue-router: 您的项目中目前没有包含路由库，我们需要先将它添加进来。
> 2. 创建路由配置: 我会创建一个新文件 src/router/index.ts，用来定义不同页面对应的 URL 路径。
> 3. 创建“页面”文件夹: 我会新建一个 src/views 文件夹，专门用来存放您的页面级组件。
> 4. 迁移当前页面: 您当前的 App.vue 的所有内容，将被迁移到一个新的 src/views/HomePage.vue 文件中，作为我们的第一个页面。
> 5. 创建菜单组件: 我会为您创建一个新的菜单组件 src/components/AppMenu.vue。
> 6. 改造主布局: 我会修改 src/layout/Layout.vue，在 Header 下方集成新的菜单组件，并放置一个 <router-view> 作为页面的“容器”。
> 7. 更新主程序入口: 最后，我会更新 main.ts 和 App.vue，让整个应用加载并使用我们刚刚创建的路由系统。

 这是一个清晰且模块化的重构方案。如果您同意这个计划，我们就从第一步开始。